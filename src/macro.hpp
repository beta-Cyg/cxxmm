#ifndef MACRO_HPP
#define MACRO_HPP

#ifndef BUFFER_SIZE
#define BUFFER_SIZE 512
#endif

static char COMPILER[BUFFER_SIZE];
static char OPTION[BUFFER_SIZE];
static char SUFFIX[BUFFER_SIZE];
static char USER_OPTION[BUFFER_SIZE];

#endif
